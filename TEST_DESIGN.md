# テスト設計 (TEST_DESIGN.md) - 機能改善 v8 対応版

## 1. 評価パターンとテスト項目

| カテゴリ | 評価項目 | 確認操作 | 期待値 |
| :--- | :--- | :--- | :--- |
| **ログイン永続化** | リロード後の自動同期復旧 | Googleログイン後、ブラウザをリロード(F5)する | ポップアップなしで自動的に「同期中」→「同期済み」状態に戻ること |
| | ログアウト時の挙動 | 「ログアウト」ボタンをクリックし、リロードする | トークンが破棄され、自動同期が開始されない（ログインボタンが出る）こと |
| **役職フィルタ** | フィルタの切り替え | カレンダー上の役職フィルタを操作する | カレンダーの表示ドット、および下部/サイドのリストが連動して切り替わること |
| | 会議体モーダル連動 | フィルタ選択状態で「会議体から追加」を開く | モーダル内の会議体リストが、現在選択中の役職用のものに絞り込まれていること |
| | フィルタ解除 | フィルタで「（全表示）」を選択する | 役職に関わらずすべての予定・タスクが表示されること |
| **予定・タスク編集** | カレンダーでのタスク編集 | カレンダー画面に表示されているタスクをタップする | 編集フォームが開き、内容やステータス（完了等）を変更・保存できること |
| | 予定のステータス変更 | 予定を「完了」ステータスにする | 保存後、カレンダー上のドットが薄くなり、タイトルに打消し線が入ること |
| **データ整合性** | クラウド保存 | カレンダーでタスク/予定を編集後、別端末で確認 | 変更内容が正常にクラウドへアップロードされ、他端末へ同期されること |
| | 複数デバイス間同期 (v9) | デバイスAで変更後、デバイスBでタブを切り替える | デバイスBで自動的にリロードが走り、Aの変更が反映されること |
| | 定期自動同期 (v9) | ログイン状態で3分放置し、他デバイスで変更する | 3分後のチェックタイミングで自動的に最新化（リロード）されること |
| **タスク定義 (v10)** | 新カテゴリ「委員」 | 定型タスクで「委員」を選択し保存 | 設定画面、タスク画面で正しく「委員」として表示・分類されること |
| | テンプレート表示フィルタ | 役職を選択した状態で、役職不問のテンプレートを確認 | 対象役職が空の定型タスクがテンプレート一覧に表示されること |
| **UI・自動化 (v11)** | テンプレートUI | タスク画面の「クイック作成」一覧を確認 | 横長リスト形式になり、長いタイトルも省略されずに表示されていること |
| | 会議追加時タスク自動生成 | 会議体またはカテゴリ「会議」の予定を追加/保存 | 1週間後に「旅費精算」タスクが自動で追加され、二重登録されないこと |
| **UI改善 (v12)** | テンプレートの折りたたみ | タスク画面の「クイック作成」ヘッダーをクリック | アコーディオン形式で開閉でき、初期状態で閉じていること |
| **PWA・アラート・データ削除 (v13)** | PWA設定 | ブラウザでマニフェストを確認 | 正しく認識され、ホーム画面追加後に全画面で起動すること |
| | 期限アラート | 期限切れのタスクがある状態でダッシュボード表示 | 上部に赤い通知バナーが表示されること |
| | データ削除 | 設定から「データをすべて削除」を実行 | 全データが消去され、ログアウトして初期状態に戻ること |
| 共通 | ログアウト・データ削除 | 設定画面の削除ボタン押下で OK 押下後、localStorage がクリアされ、初期状態（役割未設定）に戻る | 正常に初期化される |
| メモ (v15.1) | リッチテキスト書式 | ツールバーの各ボタン（太字、リスト、色、引用、コード等）を押下 | エディタ内のテキストに書式が反映される |
| メモ (v15.1) | 途中保存 | 編集中に「保存」ボタンを押下 | 画面が閉じず、保存完了のフィードバック（または状態維持）があり、一覧に反映される |
| メモ (v15.1) | エディタを閉じる | 「×」または「閉じる」ボタンを押下 | エディタが閉じ、一覧画面に戻る |
| メモ (v15.1) | レスポンシブ表示 (PC) | PC サイズ（幅 768px 超）でエディタを開く | 左に一覧、右にエディタが並んで表示される |
| メモ (v15.1) | レスポンシブ表示 (SP) | スマホサイズ（幅 768px 以下）でエディタを開く | エディタが全画面（一覧を隠して）表示される |
| メモ (v15.1) | 自動タイトル生成 | タイトルを空のままテキストメモを保存 | `yyyymmdd_nn_森` の形式でタイトルが自動設定される |
| メモ (v15.1) | データ互換性 | v15 以前のプレーンテキストメモを開く | 改行が保持された状態でリッチテキストとして表示・編集できる |
| メモ (v15.1) | プレビュー表示 | リッチテキスト（HTML）を保存後、一覧を確認 | HTML タグが除去されたプレーンテキストで内容がプレビューされる |

## 2. テスト方法

### 手動テスト
1. **リフレッシュ・チェック**: ログイン状態で F5 キーを押し、数秒後に自動的に「同期済み」の緑アイコンが表示されるか確認。
2. **フィルタ・モーダル連動**: カレンダーの役職フィルタを切り替え、「会議体定義から作成」ボタンを押して、リストの中身がフィルタと一致するか確認。
3. **カレンダー画面タスク更新**: カレンダー画面に表示されている未完了タスクをタップし、「完了」に変更して保存。リストが更新され、カレンダーのドットも同期されることを確認。

### 自動ビルド確認
- `npm run build` が正常にパスすること。
